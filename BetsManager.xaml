<UserControl x:Class="SpreadTrader.BetsManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpreadTrader"
             mc:Ignorable="d" 
	         DataContext="{Binding RelativeSource={RelativeSource Self}}" d:DesignWidth="606.667" Initialized="UserControl_Initialized">
	<UserControl.Resources>
		<local:RowToIndexConverter x:Key="RowToIndexConverter" />
		<local:ColorConverter x:Key="ColorConverter" />
		<local:SideColorConverter x:Key="SideColorConverter" />
        <Style BasedOn="{StaticResource {x:Type Button}}" 
            TargetType="{x:Type Button}">
            <Setter Property="Height" Value="24"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="6,-1,6,0"/>
        </Style>
    </UserControl.Resources>
	<Grid  Background="Ivory">
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Background="Ivory">
			<TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="{Binding Path=LastUpdated}"/>

            <!--Useful debugging features-->
            
            <!--<Button Margin="30,0,0,0" Tag="Reset"  Click="Button_Click">Reset</Button>
            <Button Margin="30,0,0,0" Tag="Run"  Click="Button_Click">Run</Button>
            <Button Margin="30,0,0,0" Tag="Next"  Click="Button_Click">Next</Button>-->

            <!--<Button Margin="30,0,0,0" Tag="Back1"  Click="Button_Click">Back</Button>
            <Button Margin="30,0,0,0" Tag="Lay1"  Click="Button_Click">Lay</Button>
            <Button Margin="30,0,0,0" Tag="Match1"  Click="Button_Click">Match</Button>
            <Border Margin ="30,0,0,0" Background="DarkGray" Width="10"/>
            <Button Margin="30,0,0,0" Tag="Back2"  Click="Button_Click">Back</Button>
            <Button Margin="30,0,0,0" Tag="Lay2"  Click="Button_Click">Lay</Button>
            <Button Margin="30,0,0,0" Tag="Match2"  Click="Button_Click">Match</Button>-->

            <Button Margin="30,0,0,0" Tag="AbsoluteCancelAll"  Click="Button_Click">Absolute Cancel All</Button>
            <Button Margin="30,0,0,0" Tag="CancelAll"  Click="Button_Click">Cancel All</Button>
            <Button Margin="30,0,0,0" Tag="HalveUnmatched"  Click="Button_Click" FontSize="14">&#x00BD;</Button>
            <CheckBox Margin="16,0,0,0" IsChecked="{Binding Path=UnmatchedOnly}" VerticalAlignment="Center" Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked" >Unmatched only</CheckBox>
			<Button Margin="10,0,0,0" Visibility="Collapsed" Tag="Stream" Click="Button_Click" Content="{Binding Path=StreamingButtonText}"/>
			<TextBlock Margin="4,0,0,0" Text="Active" Padding="5,7,8,8" Background="{Binding Path=StreamingColor}"/>
			<TextBlock Text	="Status: " Padding="7,8" />
			<!--<TextBox TextWrapping="Wrap" Text="{Binding Path=DebugID}" Visibility="Hidden" HorizontalContentAlignment="Center" />-->
		</StackPanel>
		<ContentControl Grid.Row="1">
			<DataGrid Name="dataGrid" AutoGenerateColumns="False" Background="#00000000" ItemsSource="{Binding Rows}" PreviewMouseUp="dataGrid_PreviewMouseUp" CanUserAddRows="False" CanUserResizeColumns="True" CanUserResizeRows="False" Grid.ColumnSpan="2" IsReadOnly="True" CanUserSortColumns="True"
				ScrollViewer.CanContentScroll="True" 
				ScrollViewer.VerticalScrollBarVisibility="Auto"
				ScrollViewer.HorizontalScrollBarVisibility="Auto" >
				<DataGrid.ItemContainerStyle>
					<Style TargetType="DataGridRow">
						<Style.Triggers>
							<DataTrigger Binding="{Binding Path=Hidden}"  Value="True">
								<Setter Property="Visibility" Value="Collapsed"/>
                                <!--<EventSetter Event="PreviewMouseUp" Handler="DataGridRow_PreviewMouseUp"/>-->
                            </DataTrigger>
						</Style.Triggers>
					</Style>
				</DataGrid.ItemContainerStyle>
				<DataGrid.CellStyle>
					<Style TargetType="DataGridCell">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
				</DataGrid.CellStyle>
				<DataGrid.Columns>
					<DataGridTemplateColumn>
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<Button Tag="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Converter={local:RowToIndexConverter}}" Content="X" FontWeight="Bold" Background="Orange" Foreground="White" Click="RowButton_Click" />
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>

                    <!--<DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox Tag="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}}" Click="OverrideCheckBox_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>-->

                    <DataGridTemplateColumn Header="NC">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <CheckBox Tag="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}}" Click="NoCancelCheckBox_Click"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!--<DataGridCheckBoxColumn Binding="{Binding Override}"/>
                    <DataGridCheckBoxColumn Binding="{Binding NoCancel}" IsReadOnly="False" />-->
					<DataGridTextColumn Width="120" Header="Time" Binding="{Binding Time}"/>
					<DataGridTextColumn Width="1.25*" Header="BetID" Binding="{Binding BetID}"/>
					<DataGridTextColumn Width="1.5*" Header="Runner" Binding="{Binding Runner}" />
					<DataGridTextColumn Width="0.9*" Header="Type" Binding="{Binding Side}"  >
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="{x:Type TextBlock}">
								<Setter Property="Background" Value="{Binding IsBack, Converter={StaticResource SideColorConverter}}"/>
							</Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>
					<DataGridTextColumn Width="0.8*" Header="Stake" Binding="{Binding Stake, StringFormat={}{0:N2}}" />
					<DataGridTemplateColumn Header="Odds" CanUserSort="True" SortDirection="Ascending" SortMemberPath="DisplayOdds" >
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<Label Tag="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Converter={local:RowToIndexConverter}}"  Content="{Binding DisplayOdds, Mode=OneWay, StringFormat={}{0:N2}}" MouseUp="Label_MouseUp" />
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
					<DataGridTextColumn Width="1.15*" Header="Profit/Liability" Binding="{Binding Profit, StringFormat={}{0:N2}, Mode=OneWay}"/>
					<DataGridTextColumn Width="0.6*" Header="Status" Binding="{Binding IsMatchedString, Mode=OneWay}">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="{x:Type TextBlock}">
								<Setter Property="Background" Value="{Binding IsMatched, Converter={StaticResource ColorConverter}}"/>
							</Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>
				</DataGrid.Columns>
			</DataGrid>
		</ContentControl>
	</Grid>
</UserControl>
